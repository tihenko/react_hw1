export const Company = ({name, catchPhrase, bs}) => {
    return (
        <div>
            <p>name: {name}</p>
            <p>catchPhrase: {catchPhrase}</p>
            <p>bs: {bs}</p>
        </div>
    );
}
